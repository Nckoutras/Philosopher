fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.2
pgvector==0.3.2
pydantic[email]==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
httpx==0.27.2
anthropic==0.99.0
openai==1.51.0
redis[hiredis]>=4.6.0,<6.0.0
arq>=0.25.0
apscheduler==3.10.4
stripe==10.11.0
resend==2.3.0
jinja2==3.1.4
posthog==3.7.0
# Error monitoring. Pinned, not floored: the privacy posture depends on option
# defaults (max_request_body_size, the auto-enabling LLM integrations) that a
# major bump could change under us. test_observability.py pins the kwargs.
sentry-sdk==2.68.1
python-dotenv==1.0.1
pytest==8.3.3
pytest-asyncio==0.24.0
tiktoken==0.7.0
# Pinned, not floored. CI and Render each resolved this independently from a
# >=10.4.0 floor and drifted across a major version; the card renderer's output
# depends on the Pillow build (freetype, and whether the wheel carries Raqm).
Pillow==12.3.0
emoji==2.12.1
