"""§8.2 eval harness.

NOT COLLECTED BY CI. pyproject sets testpaths = ["tests"], so nothing here runs
on a PR — which is deliberate: the harness spends money on real completions.
Its data files are validated by tests/test_anti_flex_terms.py, which makes no
API calls and does run.
"""
